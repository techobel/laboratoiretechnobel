{% extends "::base.html.twig" %}

{% block title %}Mise à jour de {{ annonce.title }} {{ parent() }}{% endblock %}

{% block h3 %}Mise à jour d'une <span class='exergue'>annonce</span>{% endblock %}
    
{% block body %}
    {{ parent() }}
    
    <section id="form_contact" class="clearfix">
        <div class="container">
            {% for flashMessage in app.session.flashbag.get('notice') %}
                <div class="alert-success has-success">{{ flashMessage }}</div>
            {% endfor %}
            <div class="form row form-inline form-one clearfix">
                {% form_theme form 'form/field.html.twig' %}
                {{ form(form) }}
            </div>
        </div>
    </section>
    
{% endblock %}
